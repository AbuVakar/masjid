# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/silsila_ul_ahwaal

# JWT Configuration
JWT_SECRET=your-super-secure-jwt-secret-key-here
JWT_EXPIRE=7d

# File Upload Configuration
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads

# Rate Limiting
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX=100

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# WhatsApp Configuration
ADMIN_WHATSAPP_NUMBER=+917060813814
ADMIN_PHONE_NUMBER=+917060813814
WHATSAPP_ENABLED=true
WHATSAPP_API_URL=https://api.whatsapp.com/send

# Email Configuration (Required for Password Reset)
# Choose ONE of the following options:

# OPTION 1: Gmail (Free, but requires App Password setup)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password-here
EMAIL_SERVICE=gmail

# OPTION 2: SendGrid (Recommended - Professional, Free tier available)
# EMAIL_USER=apikey
# EMAIL_PASS=your-sendgrid-api-key
# EMAIL_SERVICE=sendgrid

# OPTION 3: Mailgun (Developer friendly)
# EMAIL_USER=your-mailgun-username
# EMAIL_PASS=your-mailgun-password
# EMAIL_SERVICE=mailgun

# OPTION 4: SMTP2GO (Simple setup)
# EMAIL_USER=your-smtp2go-username
# EMAIL_PASS=your-smtp2go-password
# EMAIL_SERVICE=smtp2go

# Frontend Configuration
FRONTEND_URL=http://localhost:3000

# Setup Instructions:
# Gmail: Enable 2FA → Generate App Password → Use App Password in EMAIL_PASS
# SendGrid: Sign up → Get API Key → Use API Key in EMAIL_PASS
# Mailgun: Sign up → Get SMTP credentials → Use in EMAIL_USER/EMAIL_PASS
# SMTP2GO: Sign up → Get SMTP credentials → Use in EMAIL_USER/EMAIL_PASS
